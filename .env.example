# Copy to .env.local (gitignored) and fill in. Never commit the real key.
# SERVER-SIDE ONLY — no VITE_ prefix, so Vite never inlines this into the client bundle.
# Read by api/candles.ts (Vercel) and the vite.config.ts dev proxy (local dev).
TWELVEDATA_KEY=your_twelve_data_key_here
